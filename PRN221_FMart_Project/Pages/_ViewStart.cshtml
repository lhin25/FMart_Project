@{
    if (!string.IsNullOrEmpty(Context.Session.GetString("IsSignIn")))
    {
        if (User.IsInRole("Admin"))
        {
            Layout = "_LayoutAdmin";
        }
        else
        {
            Layout = "_Layout";
        }
    }
}
